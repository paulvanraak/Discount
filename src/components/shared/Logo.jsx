export default function Logo({ className = '' }) {
  return (
    <span className={`font-semibold text-ink-900 ${className}`}>
      Bespaar met <span className="text-primary-500">Donnie</span>
    </span>
  )
}
